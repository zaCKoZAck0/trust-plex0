const { getMovieById } = require("./movie");

const getMovieDetails = (array) =>{
    return new Promise((resolve, reject) => {
        try{
            
        }
        catch(err){
            reject(err)
        }
    })
}

module.exports = {
    getMovieDetails
}